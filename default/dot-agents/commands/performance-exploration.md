# performance-exploration

Use julia-mcp to explore alternative implementations. Benchmark code against current implementation, using BenchmarkTools (available in base env). Dont edit code, but report to the user on performance metrics and approaches.