# User-specific window rules to fix webapp tiling issues

# Force all Google Chrome app windows to tile
windowrule = tile, class:Google-chrome, initialTitle:.*_/

# Alternative approach - force tiling for any window with app-like titles
windowrule = tile, title:.*_/

# Ensure webapps don't open as floating by default
#windowrule = tile, class:Google-chrome